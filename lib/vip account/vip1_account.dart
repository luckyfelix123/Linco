import 'package:flutter/material.dart';
class Vip1Account extends StatelessWidget {
  const Vip1Account({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(  
        child: Column(  
          children: [   

            
          ],
        ),
      ),
    );
  }
}